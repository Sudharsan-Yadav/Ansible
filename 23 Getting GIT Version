---
- hosts: dev
  connection: ssh

  tasks:
    - name: Install the SCM tool
      yum: name=git state=present

    - name: Check the GIT version
      command: git -v
      register: abc

    - debug:
        msg: "The SCM tool is {{abc.stdout}}"
