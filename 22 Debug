---
- hosts: dev
  connection: ssh

  tasks:
    - debug:
        msg: "The dev server OS is installed"

    - debug:
        msg: "The dev server OS is {{ansible_os_family}}"

    - debug:
        msg: "The dev server's kernel version is {{ansible_kernel}}"

    - debug:
        msg: "The dev server's bit size is {{ansible_architecture}}"
...
