---
- hosts: all
  connection: ssh

  tasks:
    - name: Install Webserver on RedHat
      yum: name=httpd state=present
      when: ansible_os_family=="RedHat"

    - name: Insatll Webserver on Ubuntu
      apt: name=apache2 state=present
      when: ansible_os_family=="Ubuntu"
...
